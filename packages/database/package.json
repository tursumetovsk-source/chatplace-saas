{
  "name": "@chatplace/database",
  "version": "1.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "prisma generate --schema prisma/schema.prisma && tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "prisma:generate": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^6.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "prisma": "^6.3.0",
    "typescript": "^5.7.0"
  }
}
